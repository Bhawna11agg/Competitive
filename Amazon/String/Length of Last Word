int Solution::lengthOfLastWord(const string A) {
    int length=0;
    for(int i=A.length()-1;i>=0;i--){
      if(A[i]!=' '){
          length++;
      }
      if(length!=0 && A[i]==' '){
          return length;
      }
    }
   return length;
}// InterviewBit
